TODO: turn on the yellow LED (and turn the rest off) as soon in boot process as possible.
And maybe do the same during power off, but only if it stays on until the very end

Put /boot/config.txt in repo. Possibly as part of create_symlinks.sh/install.sh


run pycheck
