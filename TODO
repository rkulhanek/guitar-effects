Put /boot/config.txt in repo. Possibly as part of create_symlinks.sh/install.sh


run pycheck

disable wifi autoconnect

date in log is showing Jan 01. Also, log being overwritten. Set it up to prune, but keep the last day or two.

There was a checklist somewhere online for realtime audio. Find it. When the CPU wasn't getting set to performance,
the audio was cutting out periodically, but was *not* reporting XRUNs. And I still *rarely* get that same behavior.
So more CPU could help.
